# Arduino WEMOS D1 project

## Getting started

1. install recommended extension `pallette -> Installing recommended extensions`
2. install arduino cli

```bash
brew update
brew install arduino-cli
```
